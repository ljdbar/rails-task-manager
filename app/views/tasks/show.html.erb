<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p></h1>
<br>
<%= link_to "-------- back to list --------", "/tasks" %>


<%= link_to "edit this task", edit_task_path(@task) %>

<%= link_to "Delete",
task_path(@task),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
